import Link from "next/link";
export function PageCTA({ label = "Ready when you are", title = "Start with a free audit.", copy = "We will show you where to focus first." }: { label?: string; title?: string; copy?: string }) { return <section className="page-cta"><span className="kicker">{label}</span><h2>{title}</h2><p>{copy}</p><Link className="button" href="/contact#audit">Request free audit</Link></section>; }
