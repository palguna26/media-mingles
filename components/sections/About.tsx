"use client";
import Image from "next/image";
import { useRef } from "react";
import { ArrowUpRight } from "lucide-react";
export function About() { const photos = useRef<HTMLDivElement>(null); const move = (e: React.PointerEvent) => { if (!photos.current || !matchMedia("(pointer:fine) and (prefers-reduced-motion:no-preference)").matches) return; const r = e.currentTarget.getBoundingClientRect(); photos.current.style.transform = `translate3d(${(e.clientX-r.left-r.width/2)/80}px, ${(e.clientY-r.top-r.height/2)/80}px, 0)`; }; return <section id="about" className="about" onPointerMove={move} onPointerLeave={() => { if (photos.current) photos.current.style.transform = ""; }}><div className="about__copy"><div className="section-label"><span>05</span><h2>The studio</h2></div><h3>SERIOUS ABOUT THE WORK.<br /><i>NOT OURSELVES.</i></h3><p>Media Mingles is an independent creative agency bringing strategy, creators, production, social and PR together. We make work that looks good, moves fast and gives people something to talk about.</p><a href="/about" className="text-link">Meet the studio <ArrowUpRight size={18} /></a><div className="about__tags">{["Strategy", "Creative", "Production", "Influence", "Distribution", "Media"].map(x => <span key={x}>{x}</span>)}</div></div><div ref={photos} className="about__photos"><div><Image src="/media/content-creators.png" alt="Media Mingles production team at work" fill sizes="(max-width: 768px) 100vw, 50vw" /></div><div><Image src="/media/unfold-brand.jpeg" alt="Behind the scenes at Media Mingles" fill sizes="(max-width: 768px) 45vw, 30vw" /></div></div></section>; }
