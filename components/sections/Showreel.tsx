"use client";
import Image from "next/image"; import { Play } from "lucide-react"; import { useLayoutEffect, useRef } from "react"; import gsap from "gsap"; import { ScrollTrigger } from "gsap/ScrollTrigger"; import { showreel } from "@/data/media";
export function Showreel() { const root = useRef<HTMLElement>(null); useLayoutEffect(() => { gsap.registerPlugin(ScrollTrigger); const ctx = gsap.context(() => { const mm = gsap.matchMedia(); mm.add("(min-width: 1024px) and (prefers-reduced-motion: no-preference)", () => gsap.timeline({ scrollTrigger: { trigger: root.current, start: "top top", end: "+=140%", scrub: .7, pin: true } }).fromTo(".showreel__poster", { yPercent: 105 }, { yPercent: 0, ease: "none" }).to(".showreel__statement", { yPercent: -18, opacity: .16, ease: "none" }, 0)); return () => mm.revert(); }, root); return () => ctx.revert(); }, []); return <section ref={root} className="showreel"><div className="showreel__statement"><span>04 / Showreel</span><h2>STRATEGY IS INVISIBLE.<br />THE RESULT SHOULD <i>NOT BE.</i></h2></div><div className="showreel__poster"><Image src={showreel.poster} alt="Media Mingles showreel poster" fill sizes="100vw" /><div className="showreel__overlay"><h3>MAKE<br />THEM<br /><i>FEEL IT.</i></h3><button type="button" disabled aria-describedby="reel-status"><Play /> Play reel</button><p id="reel-status">Showreel coming soon</p></div></div></section>; }
