"use client";
import { useLayoutEffect, useRef } from "react";
import gsap from "gsap";
import { ScrollTrigger } from "gsap/ScrollTrigger";
export function Showreel() { const root = useRef<HTMLElement>(null); useLayoutEffect(() => { gsap.registerPlugin(ScrollTrigger); const ctx = gsap.context(() => { if (matchMedia("(min-width: 901px) and (prefers-reduced-motion: no-preference)").matches) gsap.timeline({ scrollTrigger: { trigger: root.current, start: "top top", end: "+=100%", scrub: .8, pin: true } }).to(".showreel__window", { width: "100vw", height: "100vh", borderRadius: 0, rotate: 0 }).to(".showreel__overlay", { opacity: 1 }, .25); }, root); return () => ctx.revert(); }, []); return <section ref={root} className="showreel"><h2><span>STRATEGY IS INVISIBLE.</span><span>THE RESULT SHOULD <i>NOT BE.</i></span></h2><div className="showreel__window"><div className="showreel__motion" /><div className="showreel__stamp">Media Mingles · Pan-India</div><div className="showreel__overlay">MAKE<br />THEM<br /><i>FEEL IT.</i></div></div></section>; }
