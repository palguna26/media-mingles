"use client";
import Image from "next/image"; import Link from "next/link"; import { useLayoutEffect, useRef } from "react"; import gsap from "gsap";
export function Hero() { const root = useRef<HTMLElement>(null); useLayoutEffect(() => { if (matchMedia("(prefers-reduced-motion: reduce)").matches) return; const ctx = gsap.context(() => { gsap.timeline().from(".hero__image img", { scale: 1.04, duration: 1.1, ease: "power2.out" }).from(".hero__line", { yPercent: 110, duration: .8, stagger: .08, ease: "power4.out" }, .1).from(".hero__support", { y: 18, opacity: 0, duration: .65 }, .45); }, root); return () => ctx.revert(); }, []); return <section ref={root} className="hero"><div className="hero__image"><Image src="/media/unfold-brand.jpeg" alt="Media Mingles campaign portrait" fill priority sizes="100vw" /></div><div className="hero__shade" /><p className="hero__label">Independent creative agency · Bengaluru</p><h1><span><b className="hero__line">MEDIA</b></span><span><b className="hero__line">MINGLES</b></span></h1><div className="hero__support"><p>Strategy, creators and production working as one team for brands that want to be remembered.</p><div><Link className="button" href="/contact#audit">Request free audit</Link><Link className="text-link" href="#work">View our work ↓</Link></div></div></section>; }
