import type { Metadata } from "next";
import Image from "next/image";
import { cities, creatorTiers, niches, proofAssets } from "@/data/site";
import { InnerPageHero } from "@/components/ui/InnerPageHero";
import { PageCTA } from "@/components/ui/PageCTA";
export const metadata: Metadata = { title: "Reach", description: "A vetted network of 500+ creators across 30+ Indian cities.", alternates: { canonical: "/reach" } };
export default function ReachPage() { return <><InnerPageHero eyebrow="Pan-India Network" title={<>500+ CREATORS.<br /><i>ONE CONTACT.</i></>} description="Every major city, creator tier and content niche — matched to the audience your brand needs." /><section className="reach-proof"><div><span>500+</span><p>Vetted creators</p></div><div><span>30+</span><p>Cities covered</p></div><div><span>Pan-India</span><p>Campaign delivery</p></div></section><section className="image-proof image-proof--reach"><Image src={proofAssets.creators} alt="Media Mingles creator campaign reach" fill sizes="100vw" /></section><section className="reach-lists"><div><span className="kicker">Top cities</span>{cities.map(item => <strong key={item}>{item}</strong>)}</div><div><span className="kicker">Creator niches</span>{niches.map(item => <strong key={item}>{item}</strong>)}</div><div><span className="kicker">Creator tiers</span>{creatorTiers.map(item => <strong key={item}>{item}</strong>)}</div></section><section className="principles"><div className="section-label"><span>01</span><h2>How we vet</h2><span>Quality over vanity</span></div>{[["Real engagement", "Audience authenticity and comment quality."], ["Content quality", "Production value, editing and on-camera presence."], ["Brand fit", "Tone, niche and audience demographics."]].map(([title, copy], i) => <article key={title}><span>0{i + 1}</span><h3>{title}</h3><p>{copy}</p></article>)}</section><PageCTA label="Let’s match you" title="Tell us who you need to reach." /></>; }
