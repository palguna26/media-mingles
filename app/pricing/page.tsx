import type { Metadata } from "next";
import { Check } from "lucide-react";
import { InnerPageHero } from "@/components/ui/InnerPageHero";
import { PageCTA } from "@/components/ui/PageCTA";
export const metadata: Metadata = { title: "Pricing", description: "Flexible Media Mingles partnership packages shaped around your growth goals.", alternates: { canonical: "/pricing" } };
const packages = [{ name: "Starter", copy: "For brands finding their voice.", items: ["Social management foundations", "Content calendar and production", "Community management", "Monthly reporting"] }, { name: "Growth", copy: "For brands ready to scale.", items: ["Everything in Starter", "Creator campaign support", "Product-production support", "Search foundations", "Dedicated strategist"] }, { name: "Enterprise", copy: "For full-funnel partnerships.", items: ["Everything in Growth", "Scaled creator campaigns", "Videography and brand films", "Advanced SEO", "Media planning and PR"] }];
export default function PricingPage() { return <><InnerPageHero eyebrow="Partnerships" title={<>BUILT AROUND<br /><i>YOUR GOALS.</i></>} description="Every engagement starts with the problem, not a fixed bundle. These are the three ways we usually work." /><section className="pricing-grid">{packages.map((pack, i) => <article key={pack.name}><span>0{i + 1}</span><h2>{pack.name}</h2><p>{pack.copy}</p><strong>Custom scope</strong><ul>{pack.items.map(item => <li key={item}><Check size={16} />{item}</li>)}</ul><a className="text-link" href="/contact#audit">Build my package ↗</a></article>)}</section><section className="faq"><span className="kicker">Questions, answered</span>{[["Is there a minimum contract?", "Most partnerships need an initial ramp-up period before moving to a rolling arrangement."], ["Can services be mixed?", "Yes. Every package is shaped around the audit and the brand’s current priorities."], ["Do you work across India?", "Yes. Remote delivery and the creator network cover India, with production arranged in major cities."]].map(([q, a]) => <details key={q}><summary>{q}</summary><p>{a}</p></details>)}</section><PageCTA label="Still deciding?" title="Start with the problem, not a package." /></>; }
